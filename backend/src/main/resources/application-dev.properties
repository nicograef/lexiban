# ── Dev profile ──
# Activate with: mvn spring-boot:run -Dspring-boot.run.profiles=dev

# CORS origins for local dev (read by CorsConfig.java).
# Not needed in production — nginx reverse proxy serves same origin.
app.cors.allowed-origins=http://localhost,http://localhost:5173
