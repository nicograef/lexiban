{
  "name": "lexiban-infra",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy:dev": "cdk deploy --all -c stage=dev",
    "deploy:prod": "cdk deploy --all -c stage=prod",
    "destroy:dev": "cdk destroy --all -c stage=dev",
    "destroy:prod": "cdk destroy --all -c stage=prod"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "~5.7.0"
  }
}
